# Get Your Code Now — Backend

Node.js + Express backend powered by Google's official @google/genai SDK.

## Setup

```bash
npm install
npm start
```

Create a `.env` file from `.env.example` and set `GEMINI_API_KEY` and `FRONTEND_ORIGIN`.

Health check: `GET /api/health`
Generate code: `POST /api/generate`
